import 'package:flutter/material.dart';

class PostAnnouncementPage extends StatelessWidget {
  const PostAnnouncementPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("İlan Ekle"),
        centerTitle: true,
      ),
      body: const Text("data"),
    );
  }
}
